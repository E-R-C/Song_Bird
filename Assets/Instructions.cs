using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class Instructions : MonoBehaviour {

	// Use this for initialization
	void Start () {
        Physics.gravity = new Vector3(1f, 0f, -9.81f);
    }
	
	// Update is called once per frame
	void Update () {
		
	}
}
